name: websocket_client
description: Transfer files between peers using websockets

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.2.5
  path_provider: ^2.0.13

  english_words: ^4.0.0
  provider: ^6.0.0
  web_socket_channel: ^2.3.0
  connectivity_plus: ^3.0.3
  data_connection_checker: ^0.3.4
  shelf_web_socket: ^1.0.3
  shelf: ^1.4.0
  universal_html: ^2.0.9
  shelf_plus: ^1.7.0
  angel3_framework: ^7.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
